name: .NET

on:
  push:
    branches: [ main ]
  pull_request:

env:
  JAVA_VERSION: '21'
  DOTNET_VERSION: '10.0.x'

jobs:
  build:
    name: Build, Test and Analyse
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      with:
        fetch-depth: 0 # Shallow clones disabled for a better relevancy of SC analysis

    - name: Setup .NET ${{ env.DOTNET_VERSION }}
      uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Set up JDK ${{ env.JAVA_VERSION }}
      uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'microsoft'

    - name: Cache SonarCloud packages
      uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4
      with:
        path: ~\sonar\cache
        key: ${{ runner.os }}-sonar
        restore-keys: ${{ runner.os }}-sonar

    - name: Install SonarCloud scanners
      run: dotnet tool install --global dotnet-sonarscanner

    - name: Install dotnet reportgenerator
      run: dotnet tool install --global dotnet-reportgenerator-globaltool

    - name: Restore dependencies
      run: dotnet restore GovUK.Dfe.FlexForms.Web.sln

    - name: Build, Test and Analyze
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: |
        dotnet-sonarscanner begin /d:sonar.qualitygate.wait=true /d:sonar.scanner.skipJreProvisioning=true /k:"DFE-Digital_flexforms-web" /o:"dfe-digital" /d:sonar.token="${{ secrets.SONAR_TOKEN }}" /d:sonar.host.url="https://sonarcloud.io" /d:sonar.coverageReportPaths=CoverageReport/SonarQube.xml /d:sonar.coverage.exclusions="**/Program.cs,**/wwwroot/**,**/*.cshtml,**/*.cshtml.cs,**/*.g.cs,**/DependencyInjection.cs,**/MessagingEventBusConfigurator.cs,**/*WorkState.cs,**/*Messages.cs,**/Controllers/**,**/AutocompleteService.cs,**/Middleware/**,**/Authentication/**,**/Messaging/**,**/Security/*Strategy.cs,**/Security/*Configurator.cs,**/Security/*Helper.cs,**/Security/UserPermissionsCache.cs,**/Security/PermissionsClaimProvider.cs,**/Services/Platform/**,**/Services/CypressAuthenticationService.cs,**/Services/ExternalAppsCypressRequestChecker.cs,**/Services/FieldViewModel.cs,**/Services/TestTokenHandler.cs,**/Services/TestAuthenticationService.cs,**/Services/ITestAuthenticationService.cs,**/ServiceCollectionExtensions.cs,**/TenantAwareServiceCollectionExtensions.cs,**/PlatformBootstrapExtensions.cs,**/ConfigureCookieTicketStore.cs,**/DistributedCacheTicketStore.cs,**/Pages/Shared/BaseFormPageModel.cs,**/Pages/FormEngine/RenderForm.Support.cs,**/ContributorService.cs,**/JsonFormTemplateParser.cs,**/ApiTemplateStore.cs,**/JsonFileTemplateStore.cs,**/FormTemplateProvider.cs,**/GovUK.Dfe.FlexForms.PlaywrightTests/**" /d:sonar.issue.ignore.multicriteria=e1,e2 /d:sonar.issue.ignore.multicriteria.e1.ruleKey=Web:S5254 /d:sonar.issue.ignore.multicriteria.e1.resourceKey=**/_SummaryRow.cshtml /d:sonar.issue.ignore.multicriteria.e2.ruleKey=Web:ItemTagNotWithinContainerTagCheck /d:sonar.issue.ignore.multicriteria.e2.resourceKey=**/_SummaryRow.cshtml
        dotnet build GovUK.Dfe.FlexForms.Web.sln --no-restore
        dotnet test GovUK.Dfe.FlexForms.Web.sln --no-build --verbosity normal --filter "TestCategory!=Playwright" --collect:"XPlat Code Coverage"
        reportgenerator -reports:./**/coverage.cobertura.xml -targetdir:./CoverageReport -reporttypes:SonarQube
        dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
